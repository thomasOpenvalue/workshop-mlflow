name: workshop-mlflow

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      alpha: # precise type and default value
      l1_ratio: # precise type and default value
    command: # bash command like "python ... {...}"